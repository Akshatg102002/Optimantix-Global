
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to all documents
    // But only authenticated users can write (create, update, delete)
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
